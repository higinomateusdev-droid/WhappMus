# Visual verification

- Desktop preview: the private dashboard renders with the intended dark laboratory visual system, persistent sidebar, status controls, metric cards, message pipeline, and terminal panel.
- Mobile preview: the sidebar collapses away, the header remains readable, metric cards stack cleanly, and the pipeline flows vertically without clipping.
- Authentication: the active preview session reached the authenticated dashboard successfully.
